# List slicing

mixture = [1, 2, 3, "one", "two", "three"]
print(type(mixture))

print(mixture[1:3])  # This will return [2,3]
print(mixture[::2])  # will print every other one (so will only print every other one


